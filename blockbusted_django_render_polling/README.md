# Blockbusted (Django + DRF + SimpleJWT) — Render + Polling Edition

This project is optimized for free-tier deploy on Render using a simple HTTP API with polling.
It includes a minimal HTML landing page that loads your CSS from `static/css/`.
